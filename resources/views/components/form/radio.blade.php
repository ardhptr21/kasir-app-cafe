<label class="inline-flex items-center">
    <input type="radio" class="w-5 h-5" name="{{ $name }}" value="{{ $value }}"
        {{ $attributes }}>
    <span class="ml-2 text-gray-700">{{ $label }}</span>
</label>
